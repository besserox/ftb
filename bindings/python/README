This directory contains the python bindings for FTB.

Python examples using these bindings are available in the components/examples directory.

Note:
1. These bindings have been tested with Python version 2.5.2
